########################################
# TOOLS configuration
########################################
# Вся логика тулченджера вынесена в расширение zbolt_toolchanger:
#   klippy/extras/zbolt_toolchanger.py
#   klippy/extras/zbolt_toolchanger_tool.py
# Модули и документация живут в репозитории прошивки Z-Bolt/klipper:
# docs/zbolt/zbolt_toolchanger.md — описание параметров и матрица аварий.
#
# Команды T0 и T1 регистрирует само расширение, макросов T0/T1 здесь нет.
# Скорости задаются в мм/с (как принято в Klipper); прежние F-значения
# делятся на 60: speed_fast 18000 -> 300, speed_slow 3600 -> 60.

[output_pin sol]
pin: PB15 # HEAT0
pwm: True
value: 1
shutdown_value: 1

[zbolt_toolchanger]
initial_tool: 0
# Опрос датчиков синхронизирован с реальным движением (toolhead.wait_moves),
# поэтому вместо прежней слепой задержки G4 P2000 хватает окна стабильности.
sensor_settle_time: 0.05
verify_retries: 1
travel_speed: 300
dock_speed: 60
force_speed_factor: 100                 # бывш. M220 S100 в начале park/get
z_lift_before_home: 5
z_after_home: 5
z_lift_speed: 15
home_axes_order: y, x
# home_z_position не задан — Z хомится в центре стола, как и раньше
after_home_position: 10, 290
# slider_sensor_pin: ^PXX   # сработал = на слайдере есть голова     # датчик наличия головы на слайдере (в планах)
# Общий Z-офсет (бэбистеп), переносимый между головами
global_offset_variable: z_adjust
# Сервисный режим настройки парковок: TOOLCHANGER_SERVICE_MODE
service_mode_variable: service_mode
apply_offset_gcode:
    _SET_GCODE_OFFSET_INTERNAL X={x} Y={y} Z={z}
on_fault_both_undocked: pause
on_fault_head_lost: pause
on_fault_slider_unexpected: pause
on_fault_park_failed: pause
on_fault_get_failed: pause
on_fault_unexpected_edge: pause
on_fault_sensor_unstable: error
on_fault_startup: error

[zbolt_toolchanger_tool t0]
tool_number: 0
extruder: extruder
dock_sensor_pin: ^!PA1                   # бывш. [gcode_button t0_parking]
park_xy: 8.8, -57.8                     # бывш. t0x_park / t0y_park
unpark_xy: 8.3, -57.8                   # бывш. t0x_unpark / t0y_unpark
safe_xy: 63.8, -37.8                    # бывш. t0x_park + 55 / t0y_park + 20
stage_xy: 63.8, 0                       # заход на колонну парковки от Y0
exit_xy: 63.8, 0
retreat_speed: slow
coupling: solenoid                       # электромагнит на слайдере
solenoid_pin: sol
release_delay: 0.3                       # бывш. переменная delay
current_boost: 1.5                       # бывш. SET_TMC_CURRENT x1.5
current_boost_phase: get
seat_offset_xy: 5, 15                    # бывш. _PUSH_EXTRUDER0
seat_jerk: 5                             # бывш. t0y_jerk
offset_z_variable: t0offset

[zbolt_toolchanger_tool t1]
tool_number: 1
extruder: extruder1
dock_sensor_pin: ^!PB13                  # бывш. [gcode_button t1_parking]
park_xy: 292.5, -61.0                   # бывш. t1x_park / t1y_park
unpark_xy: 292.5, -61.0                 # бывш. t1x_unpark / t1y_unpark
safe_xy: 237.5, -41.0                   # бывш. t1x_park - 55 / t1y_park + 20
stage_xy: 237.5, 0
exit_xy: 237.5, 0
retreat_speed: slow
coupling: solenoid
solenoid_pin: sol
release_delay: 0.3
current_boost: 1.5
current_boost_phase: get
seat_offset_xy: -5, 15                   # бывш. _PUSH_EXTRUDER1
seat_jerk: 5.5                           # бывш. t1y_jerk
offset_x_variable: t1_x_offset
offset_y_variable: t1_y_offset
offset_z_variable: t1_z_offset

########################################
# Совместимость со старыми именами
########################################
# Кнопки KlipperScreen и привычка операторов ссылаются на эти имена.

[gcode_macro Zoffset]
description: Макрос автокалибровки z-офсета 2-й головы. Запускайте его при снятии или замене хотендов.
gcode:
    TOOLCHANGER_CALIBRATE_OFFSET TOOL=1

[gcode_macro PARKING_STATE]
description: Показать, какая голова на слайдере, а какая в парковке.
gcode:
    TOOLCHANGER_STATUS
